# This repository places server env example under `server/.env.example`.
# Copy the server example into `server/.env` and update values for your environment.

# Example quick-start (do NOT commit secrets):
# copy server\.env.example server\.env

See `server/.env.example` for the full list of variables you should set.
